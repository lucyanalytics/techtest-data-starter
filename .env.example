# Default — works if port 5432 is free on your host machine
DATABASE_URL=postgresql://lucy:lucy@localhost:5432/lucy_test

# If port 5432 is already used by another local Postgres, uncomment these two lines
# instead, and edit docker-compose.yml to map "5433:5432"
# DATABASE_URL=postgresql://lucy:lucy@localhost:5433/lucy_test
